# Leave VITE_BACKEND_URL commented/empty — uploads go through Vite proxy (same origin)
# Only set this if backend is on a completely different server:
# VITE_BACKEND_URL=http://your-backend-server:3333
